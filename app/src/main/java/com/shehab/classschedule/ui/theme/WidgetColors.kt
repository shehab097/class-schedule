package com.shehab.classschedule.ui.theme

import androidx.glance.unit.ColorProvider

val WBg = ColorProvider(BgColor)
val WSurface = ColorProvider(SurfaceColor)
val WBlue = ColorProvider(PrimaryBlue)
val WGreen = ColorProvider(AccentGreen)
val WTextPrimary = ColorProvider(TextPrimary)
val WTextSecondary = ColorProvider(TextSecondary)
